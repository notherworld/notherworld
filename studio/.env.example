# Copy this file to `portal/.env` and paste your OpenRouter API key below.
# `.env` is gitignored — the key stays local and is only ever read server-side
# by the Vite dev proxy (never bundled into the browser).
OPENROUTER_API_KEY=
