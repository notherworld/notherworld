/target
**/*.rs.bk

# secrets — never commit API keys
.env
.env.local
*.local

# personal / machine-local Claude Code settings
.claude/settings.local.json

# node / web build artifacts
node_modules/
studio/dist/
dist/

# wasm artifact is COMMITTED (deliberately): lets node-only users + Vercel run the
# studio without a Rust toolchain. Rebuild after engine changes (see CLAUDE.md).


# local AI-session context (personal — never commit)
CLAUDE.local.md

# private working docs (feature tally + idea ledger — never commit)
FEATURES.local.md
IDEAS.local.md
